Test for 7-base_geometry

Use of integer_validator 

add module
	>>> BaseGeometry = __import__('7-base_geometry').BaseGeometry

create variable
	>>> bg = BaseGeometry()

test area() method
	>>> try:
	... 	bg.area() 
	... except Exception as e:
	... 	print(f"Exception: {e}")	
	Exception: area() is not implemented

validate with integer
	>>> bg.integer_validator("my_int", 42)

validate with negative number
	>>> try:
	... 	bg.integer_validator("width", -4)
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: width must be greater than 0

validate with 0
	>>> try:
	... 	bg.integer_validator("width", 0)
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: width must be greater than 0

validate with no parameters
	>>> try:
	... 	bg.integer_validator()
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: integer_validator() missing 2 required positional arguments: 'name' and 'value'

validate with only name
	>>> try:
	... 	bg.integer_validator("name")
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: integer_validator() missing 1 required positional argument: 'value'

validate with True 
	>>> try:
	... 	bg.integer_validator("my_int", True)
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer

validate with None
	>>> try:
	... 	bg.integer_validator("my_int", None)
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer

validate with string 
	>>> try:
	... 	bg.integer_validator("my_int", "42")
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer

validate with list
	>>> try:
	... 	bg.integer_validator("my_int", [4])
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer

validate with set
	>>> try:
	... 	bg.integer_validator("my_int", {4, 12})
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer

validate with singleton
	>>> try:
	... 	bg.integer_validator("my_int", (12,))
	... except Exception as e:
	... 	print(f"TypeError: {e}")	
	TypeError: my_int must be an integer
